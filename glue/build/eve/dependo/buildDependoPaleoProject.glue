  
  # delete previous extension
  #run file glue/build/eve/dependo/epvDependoDeletePreviousExtensionBuild.glue


  # create modules
  run file glue/build/eve/dependo/epvDependoModules.glue
  
  # Import endogenous parvoviral element (EPV) sequences
  run file glue/build/eve/dependo/epvDependoImportSources.glue
  
  # Import data for EVE sequences
  run file glue/build/eve/dependo/epvDependoImportData.glue
  
  # Declare references 
  run file glue/build/eve/dependo/epvDependoReferences.glue


  # Set up constrained alignment tree for the Dependoparvoviruses
  run file glue/build/eve/dependo/epvDependoAlignmentTree.glue

  # Import genus level alignments and define constrained alignment tree
  run file glue/build/eve/dependo/epvDependoAlignmentsRoot.glue
  
  # Record feature coverage in root and clade-level alignments
  run file glue/build/eve/dependo/epvDependoRecordFeaturePresence.glue


  # Set up constrained alignment tree for the Dependoparvovirus EPV subclades
  run file glue/build/eve/dependo/epvDependoAlignmentTreeDigs.glue


  # Validate
  validate
